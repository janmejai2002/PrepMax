Run the full test suite (npm test). Fix any failures or log them as known issues in docs/STATE.md. Update docs/STATE.md with: what was completed this session, what is in flight, and the exact next step for the next session. Commit all changes with a conventional commit message (feat:/fix:/chore: prefix). Push to GitHub. Summarize the session in 3–5 plain English sentences that a non-technical person can understand.
